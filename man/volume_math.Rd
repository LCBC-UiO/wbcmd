% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/volume_math.R
\name{volume_math}
\alias{volume_math}
\title{Evaluate expression on volume files}
\usage{
volume_math(
  expression,
  volume_out,
  var,
  fixnan = FALSE,
  verbose = get_wb_verbosity()
)
}
\arguments{
\item{expression}{Math expression string (e.g. \code{"(x - y) / y * 100"}).}

\item{volume_out}{Path for the output volume file.}

\item{var}{Named list of input volume file paths. Names correspond to
variable names used in \code{expression}.}

\item{fixnan}{Logical; replace NaN results with 0?}

\item{verbose}{Logical; print command output?}
}
\value{
The result of the underlying \code{\link[=wb_cmd]{wb_cmd()}} call.
}
\description{
Wraps \code{wb_command -volume-math} to perform element-wise mathematical
operations on volume files at each voxel.
}
\examples{
\dontrun{
volume_math(
  "clamp(x, 0, 100)",
  volume_out = "clamped.nii.gz",
  var = list(x = "input.nii.gz")
)
}

}
