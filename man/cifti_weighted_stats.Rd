% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cifti_weighted_stats.R
\name{cifti_weighted_stats}
\alias{cifti_weighted_stats}
\title{Compute weighted statistics on a CIFTI file}
\usage{
cifti_weighted_stats(
  cifti_in,
  operation = NULL,
  percentile = NULL,
  spatial_weights = FALSE,
  cifti_weights = NULL,
  column = NULL,
  roi = NULL,
  show_map_name = FALSE,
  verbose = get_wb_verbosity()
)
}
\arguments{
\item{cifti_in}{Path to the input CIFTI file.}

\item{operation}{One of \code{"MEAN"}, \code{"STDEV"}, \code{"SAMPSTDEV"}, \code{"SUM"}.}

\item{percentile}{Numeric percentile (alternative to \code{operation}).}

\item{spatial_weights}{Logical; use vertex areas and voxel volumes as
weights?}

\item{cifti_weights}{Path to a CIFTI file to use as weights.}

\item{column}{Integer; compute for a single column only (1-indexed).}

\item{roi}{Path to a CIFTI ROI file.}

\item{show_map_name}{Logical; print map name before each result?}

\item{verbose}{Logical; print command output?}
}
\value{
Character vector of output lines (stats values).
}
\description{
Wraps \code{wb_command -cifti-weighted-stats} to compute spatially-weighted
statistics along CIFTI columns.
}
\examples{
\dontrun{
cifti_weighted_stats("data.dscalar.nii", operation = "MEAN",
                     spatial_weights = TRUE)
}

}
