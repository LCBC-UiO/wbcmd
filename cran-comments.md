## R CMD check results

0 errors | 0 warnings | 1 note

* This is a new submission.

## System requirements

This package wraps the 'Connectome Workbench' command-line tool ('wb_command').
All examples and tests that require the external binary are guarded with
`\dontrun{}` or conditionally skipped, so checks pass without it installed.
