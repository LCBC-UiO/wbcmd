# Get Connectome Workbench version

Get Connectome Workbench version

## Usage

``` r
wb_version()
```

## Value

Character string with the workbench version, or an empty string if
unavailable.

## Examples

``` r
if (FALSE) { # \dontrun{
wb_version()
} # }
```
